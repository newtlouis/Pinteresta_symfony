{% extends 'base.html.twig' %}

{% block body %}

    <article>
        <h2>{{ pin.title }}</h2>
        <div class="metadata">écrit le {{pin.createdAt | date('d/m/Y')}} à {{pin.createdAt | date('H:i')}} dans la catégorie politique</div>
        <div class="content">
            <img src={{pin.image}} alt="image_test">
            {{pin.content | raw}}
            
        </div>
	</article>

{% endblock %}
